/**
 * Frontend Configuration
 *
 * Sau khi chạy tunnel, bạn sẽ nhận được URL dạng:
 *   https://xxxx-xxxx-xxxx.trycloudflare.com
 *
 * Copy URL đó và paste vào bên dưới.
 */

const API_CONFIG = {
  // Dán URL tunnel vào đây (ví dụ: https://abc-123.trycloudflare.com)
  API_BASE_URL: 'https://leave-wellington-circles-truck.trycloudflare.com',

  // Socket.IO URL (để trống = tự dùng API_BASE_URL)
  SOCKET_URL: '',
};
